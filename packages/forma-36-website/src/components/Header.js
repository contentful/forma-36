import React from 'react';
import Logo from './Logo';
import Container from './Container';

const Header = () => (
  <div>
    <Container>
      <Logo />
    </Container>
  </div>
);
export default Header;
