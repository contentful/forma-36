---
title: 'Form'
type: 'component'
status: 'stable'
section: 'formComponents'
slug: /components/form/
github: 'https://github.com/contentful/forma-36/tree/forma-v4/packages/components/forms/src/form'
storybook: 'https://v4-f36-storybook.netlify.app/?path=/story/form-elements-form--basic'
---

`Form` is a wrapper component to apply a `<form />` wrapper and handle form submissions.

## How to use Form

- The `Form` component should wrap form elements into `<form />`
- Provide an `onSubmit` handler for form validation and submission
- state of the validation of the form should be manipulated from outside

Go ahead and add some fields into your form:

- [Form control](/components/form-control/)
- [Text input](/components/text-input/)
- [Textarea](/components/textarea/)
- [Checkbox](/components/checkbox/)
- [Radio](/components/radio/)
- [Select](/components/select/)
- [Switch](/components/switch/)

## Code examples

### Basic example

```jsx
function FormaExample() {
  const [submitted, setSubmitted] = React.useState(false);
  return (
    <Form onSubmit={() => setSubmitted(true)}>
      <FormControl>
        <FormControl.Label isRequired>Name</FormControl.Label>
        <TextInput />
        <FormControl.HelpText>
          Please enter your first name
        </FormControl.HelpText>
      </FormControl>

      <FormControl>
        <FormControl.Label>Description</FormControl.Label>
        <Box>
          <Textarea />
        </Box>
        <FormControl.HelpText>Tell me about yourself</FormControl.HelpText>
      </FormControl>
      <Button variant="primary" type="submit" isDisabled={submitted}>
        {submitted ? 'Submitted' : 'Click me to submit'}
      </Button>
    </Form>
  );
}
```

### Using character count

```jsx
function FormaExample() {
  const [submitted, setSubmitted] = React.useState(false);
  return (
    <Form onSubmit={() => setSubmitted(true)}>
      <FormControl>
        <FormControl.Label isRequired>user name</FormControl.Label>
        <TextInput />
        <FormControl.HelpText>Please enter your username</FormControl.HelpText>
      </FormControl>
      <FormControl>
        <FormControl.Label isRequired>password</FormControl.Label>
        <TextInput maxLength={10} />
        <Flex justifyContent="space-between">
          <FormControl.HelpText>
            Password can only have 10 characters
          </FormControl.HelpText>
          <FormControl.Counter />
        </Flex>
      </FormControl>
      <Button variant="primary" type="submit" isDisabled={submitted}>
        {submitted ? 'Submitted' : 'Click me to submit'}
      </Button>
    </Form>
  );
}
```

### Integrations

It is possible to integrate the form with solutions like [Formik](/integrations/formik/) or [React form hook](/integrations/react-hook-form/)
