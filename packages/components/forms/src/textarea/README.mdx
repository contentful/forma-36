---
title: 'Textarea'
type: 'component'
status: 'stable'
slug: /components/textarea/
github: 'https://github.com/contentful/forma-36/tree/master/packages/components/forms/src/textarea'
storybook: 'https://f36-storybook.contentful.com/?path=/story/form-elements-textarea--basic'
typescript: './TextArea.tsx'
---

`Textarea` is a form component that allows the user to enter a sizeable amount of multi-line plain text.

## Table of contents

- [How to use Textarea](#how-to-use-textarea)
- [Examples](#examples)
  - [isRequired](#isrequired)
  - [isDisabled](#isdisabled)
  - [isInvalid](#isinvalid)
- [Accessibility](#accessibility)

## How to use Textarea

Textarea can be imported from `@contentful/f36-components` or `@contentful/f36-forms`.
This component is renders a form element, therefore it should be used within the context of a `<Form>`.

## Examples

```tsx
<Textarea id="textarea" name="description" label="description" />
```

### `isRequired`

```tsx
<Textarea id="textarea" name="description" label="description" isRequired />
```

By using the `isRequired` prop, it’s possible to control the required state of the `textarea`.
This prop will set `disabled` attribute of the HTML tag to `true`.

### `isDisabled`

```tsx
<Textarea id="textarea" name="description" label="description" isDisabled />
```

By using the `isDisabled` prop, it’s possible to control the disabled state of the `textarea`.
This prop will handle the disabled styles and set `disabled` attribute of the HTML tag to `true`.

### `isInvalid`

```tsx
<Textarea id="textarea" name="description" label="description" isInvalid />
```

By using the `isInvalid` prop, it’s possible to control the error state of the `textarea`.
This prop will handle the error styles.

### Using ref to control `textarea` tag directly

```tsx
const UsingRef = () => {
  const textareaRef = React.createRef<HTMLTextAreaElement>();

  return (
    <>
      <Textarea
        id="textarea"
        name="textarea"
        label="My textarea"
        placeholder="Placeholder"
        ref={textareaRef}
      />
      <Flex marginTop="spacingL">
        <Button onClick={() => textareaRef.current.focus()}>
          Focus Textarea with ref
        </Button>
      </Flex>
    </>
  );
};
```

The `Textarea` is exposing the ref of the `textarea` tag as a prop,
which means that you can pass a ref created with `React.createRef()` to it and it will work as expected.

## Accessibility

- To ensure accessibility, provide a `name` prop
- In case the input is required, use `isRequired` prop
- Provide validation to the input and use `isInvalid` to control the error state of `Textarea`. Also show meaninful error messages with the `ValidationMessage` component.
- Provide instructions of how to fill the field with `HelpText` component
