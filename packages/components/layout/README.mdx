---
title: 'Layout'
type: 'layout'
status: 'stable'
slug: /components/layout/
github: 'https://github.com/contentful/forma-36/tree/main/packages/components/layout'
storybook: 'https://f36-storybook.contentful.com/?path=/story/components-layout--default'
typescript: ./src/Layout.tsx, .src/LayoutBody.tsx, .src/LayoutHeader.tsx, .src/Layout.Sidebar.tsx
---

A primitive for implementing common layout patterns seen within Contentful apps.

## Introduction

** Caution **
The Layout component is meant for usage on the highest level around a page. Do not use it inside Modals and be careful when nesting layouts.

## Import

```jsx static=true
import { Layout } from '@contentful/f36-components';
// or
import { Layout } from '@contentful/f36-layout';
```

## Examples

### Layout with header and sidebars

```jsx file=./examples/LayoutExampleWithAllSubComponents.tsx

```

### Full Screen Layout with header and sidebars

```jsx file=./examples/LayoutExampleWithAllSubComponentFullScreen.tsx

```

### Basic Layout with body only

```jsx file=./examples/LayoutExampleWithNoSubComponents.tsx

```

### Layout with header

```jsx file=./examples/LayoutExampleWithHeader.tsx

```

### Layout with left sidebar

```jsx file=./examples/LayoutExampleWithLeftSidebar.tsx

```

### Layout with right sidebar

```jsx file=./examples/LayoutExampleWithRightSidebar.tsx

```

## Layout areas

The `Layout` component offers 4 individually setable areas, three of them can be set via the props `header`, `leftSidebar` and `rightSidebar`

| Prop                          | Description                                                                                                                                                                                                                   | Recommended child components             |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| `children`                    | Elements handed over as children to the Layout component will be rendered as the Layouts body. Ideally all children should be wrapped inside `Layout.Body`                                                                    | `Layout.Body`                            |
| `header`                      | Elements in the header will be overarching the whole width of the layout component. Child-Components should be wrapped inside `Layout.Header` and it is also recommended to use `Header` as the only child of `Layout.Header` | `Layout.Header` with `Header` as a child |
| `leftSidebar`, `rightSidebar` | These child-components will be rendered on the left and right side of the body content. They should be wrapped in `Layout.Sidebar`                                                                                            | `Layout.Sidebar`                         |

** Note ** There will be a seperating Line be displayed underneath the Header if there are Sidebars present. Without Sidebars, there will be no sperating line.

## Accessibility

- The Layout component renders a `<section>` by default. If you use multiple layouts or want to improve landmark navigation, provide a unique aria-label
- `Layout.Sidebar` renders as a semantic `<aside>` by default
- `Layout.Header` renders as a semantic `<header>` by default
- Keyboard navigation and focus management for interactive content inside the layout should be implemented by the consumer.

## Props (API reference)

### Layout

<PropsTable of="Layout" />

### Layout.Body

<PropsTable of="LayoutBody" />

### Layout.Header

<PropsTable of="LayoutHeader" />

### Layout.Sidebar

<PropsTable of="LayoutSidebar" />
