import { generateForma36Icon } from '@contentful/f36-icon';
import { ArrowClockwiseIcon as ArrowClockwise } from '@phosphor-icons/react';

export const ArrowClockwiseIcon = generateForma36Icon(ArrowClockwise);
