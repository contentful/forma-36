---
title: 'DragHandle'
type: 'component'
slug: /components/drag-handle/
github: 'https://github.com/contentful/forma-36/tree/forma-v4/packages/components/drag-handle'
typescript: ./src/DragHandle.tsx
v4Doc: true
---

import { Stack, TextLink } from '@contentful/f36-components';
import { Props } from '@contentful/f36-docs-utils';

DragHandle is a new component to make it easier when creating draggable elements. it contains the styling for focus, hover and dragging states.  
This component only needs to be used if you're creating a custom component with drag, some of our components already have this implemented, for example [Card](/components/card/).

## Table of contents

- [Import](#import)
- [Examples](#examples)
  - [Card example](#card-example)
- [Props](#props-api-reference)
- [Accessibility](#accessibility)
- [Help improve this page](#help-improve-this-page)

## Import

```js static=true
import { DragHandle } from '@contentful/f36-components';
// or
import { DragHandle } from '@contentful/f36-drag-handle';
```

## Examples

The DragHandle can be styled based on the props you pass to it:

**Base**:

```jsx
<DragHandle label="Drag handle" />
```

**Active**:

```jsx
<DragHandle isActive label="Active drag handle" />
```

**Focused**:

```jsx
<DragHandle isFocused label="Focus drag handle" />
```

**Hovered**:

```jsx
<DragHandle isHovered label="Hovered drag handle" />
```

### Card example

```jsx
<Card title="Title" withDragHandle>
  <Text>This is a card example with DragHandle</Text>
</Card>
```

## Props (API reference)

<Props of="DragHandle" />

## Accessibility

- You should pass a relevant label to DragHandle for screen readers, the label is not rendered.
