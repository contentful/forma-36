---
title: 'ToggleButton'
type: 'component'
status: 'stable'
section: 'buttonComponents'
slug: /components/toggle-button/
github: 'https://github.com/contentful/forma-36/tree/main/packages/components/button/src/ToggleButton'
typescript: ./ToggleButton.tsx
storybook: 'https://f36-storybook.contentful.com/?path=/story/components-button-togglebutton--basic'
---

Toggle a contextual option or view on or off, such as setting text to bold or
italic. Use `Button` for actions that change system state and `Switch` for
persistent settings.

## Import

```js static=true
import { ToggleButton } from '@contentful/f36-components';
// or
import { ToggleButton } from '@contentful/f36-button';
```

## Examples

### Basic

Example of simple usage:

```jsx file=../../examples/ToggleButtonBasicExample.tsx

```

### States

Example of active and disabled toggle buttons:

```jsx file=../../examples/ToggleButtonStatesExample.tsx

```

### Icon only

Toggle buttons can display only an icon in the `tiny`, `small`, and `medium`
sizes. Always provide an `aria-label` that describes the toggled state or
action when no visible label is present.

```jsx file=../../examples/ToggleButtonIconOnlyExample.tsx

```

### Grouped

You can group several `ToggleButtons` using `ButtonGroup` component.

```jsx file=../../examples/ToggleButtonGroupedExample.tsx

```

## Props (API reference)

<PropsTable of="ToggleButton" />
