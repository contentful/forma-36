---
title: 'GlobalStyles'
type: 'component'
slug: /components/globalstyles/
github: 'https://github.com/contentful/forma-36/tree/master/packages/core/src/GlobalStyles'
typescript: ./GlobalStyles.tsx
---

import { Stack, TextLink } from '@contentful/f36-components';
import { Props } from '@contentful/f36-docs-utils';

GlobalStyles is a component that set the common style, and also normalize the browser styles.

## Import

```js static=true
import { GlobalStyles } from '@contentful/f36-components';
// or
import { GlobalStyles } from '@contentful/f36-core';
```

## Examples

To use the GlobalStyles just add it to the render of the app.

```js static=true
import ReactDOM from 'react-dom';
import { GlobalStyles } from '@contentful/f36-components';

import App from './App';

const rootElement = document.getElementById('root');
ReactDOM.render(
  <>
    <GlobalStyles />
    <App />
  </>,
  rootElement,
);
```

## Props (API reference)

<Props of="GlobalStyles" />
