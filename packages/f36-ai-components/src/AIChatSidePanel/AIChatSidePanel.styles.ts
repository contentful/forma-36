import { css } from 'emotion';

export const getStyles = () => {
  return {
    root: css({
      width: '100%',
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      padding: '0px 10px',
    }),
  };
};
