import React, { Component } from 'react';
import cn from 'classnames';

import styles from './{{pascalCase name}}.css';

export interface {{pascalCase name}}Props {
  /**
   * Class names to be appended to the className prop of the component
   */
  className?: string;
  /**
   * Child nodes to be rendered in the component
   */
  children?: React.ReactNode;
  /**
   * An ID used for testing purposes applied as a data attribute (data-test-id)
   */
  testId?: string;
};

export function {{pascalCase name}}({className, children, testId = 'cf-ui-{{kebabCase name}}', ...otherProps}: {{pascalCase name}}Props) {
  const classNames = cn(styles.{{pascalCase name}}, className);

  return (
    <div {...otherProps} className={classNames} data-test-id={testId}>
      {children}
    </div>
  );
}

{{pascalCase name}}.defaultProps = defaultProps;

export default {{pascalCase name}};
