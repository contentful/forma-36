import React from 'react';
import cn from 'classnames';

const styles = require('./{{pascalCase name}}.css');

export interface {{pascalCase name}}Props {
  extraClassNames?: string;
  children?: React.ReactNode;
  testId?: string;
}

class {{pascalCase name}} extends React.Component<{{pascalCase name}}Props> {

  static defaultProps = {
    extraClassNames: undefined,
    testId: 'cf-ui-{{kebabCase name}}',
  };

  render() {
    const { extraClassNames, children, testId, ...otherProps } = this.props;

    const classNames = cn(styles.{{pascalCase name}}, extraClassNames);

    return (
      <div className={classNames} data-test-id={testId} {...otherProps}>
        {children}
      </div>
    );
  }
}

export default {{pascalCase name}};
