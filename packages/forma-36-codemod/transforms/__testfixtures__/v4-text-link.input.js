import React from 'react';
import { TextLink } from '@contentful/forma-36-react-components';

<TextLink disabled={false} linkType="muted" />;

<TextLink disabled linkType="secondary" />;

<TextLink href="https://contentful.com" />;
