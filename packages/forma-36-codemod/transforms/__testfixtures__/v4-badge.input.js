import React from "react";
import { Tag, Heading } from "@contentful/forma-36-react-components";
import { Button } from "@contentful/f36-components";

<Tag tagType="primary" className="className">primary</Tag>;

<Tag tagType="muted" className="className">primary</Tag>;

<Tag size="small" className="className">primary</Tag>;

<Tag tagType="primary-filled" size="small" className="className">primary filled</Tag>;

<Tag entityStatusType="draft" className="className">Draft</Tag>;

<Heading>heading</Heading>;

<Button buttonType="primary">primary button</Button>;
