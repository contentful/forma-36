import { css } from '@emotion/css';
import tokens from '@contentful/f36-tokens';

export const getStyles = () => {
  return {
    {{toCamelCase componentName}}: css({
      color: tokens.red600,
    }),
  };
};
