---
'@contentful/f36-table': minor
---

Extends the Table component with a third layout option `scrollable` which allows for overflow on the x achis. Additonally in combination with the new layout option, engineers can define the first column as sticky to enable for better orientation inside the scrolling table.
